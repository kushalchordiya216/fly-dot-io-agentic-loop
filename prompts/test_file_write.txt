You are testing the `file_write` tool.

Your task:
1. Use `file_write` to create a new file at `data/greeting.txt` with the following content:
   Hello from the file_write tool!
   This file was created by an LLM agent.

2. After creating it, use `file_read` to read the file and confirm it was written correctly.

3. Report the result — did the write succeed? What does the file contain?
