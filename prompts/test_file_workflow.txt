You are testing a multi-tool workflow.

Steps:
1. Use `file_list` to see what's in the `data/` directory.
2. Read `data/config.json` with `file_read`.
3. Use `file_edit` to change the port from 8080 to 9090.
4. Use `file_grep` to verify the old port value of 8080 no longer appears anywhere in data/config.json.
5. Use `file_write` to create a new file `data/output-summary.txt` summarizing what you did.
6. Read `data/output-summary.txt` to confirm it was written.

Report the final contents of both modified files.
